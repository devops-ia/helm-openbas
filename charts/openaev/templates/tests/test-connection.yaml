{{ if .Values.testConnection }}
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "openaev.fullname" . }}-test-connection"
  labels:
    {{- include "openaev.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      {{- if .Values.global.imageRegistry }}
      image: "{{ .Values.global.imageRegistry }}/busybox"
      {{- else }}
      image: busybox
      {{- end }}
      command: ['wget']
      args: ['{{ include "openaev.fullname" . }}-server:{{ .Values.service.port }}']
  restartPolicy: Never
{{ end }}
